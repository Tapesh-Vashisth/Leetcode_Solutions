#include <iostream>
#include <vector>
#include <map>
using namespace std;

bool isRectangleCover(vector<vector<int>>& rectangles) {
    for (auto it: rectangles) {
        int x1, x2, y1, y2;
        x1 = it[0];
        x2 = it[2];
        y1 = it[1];
        y2 = it[2];

        
    }       
}

int main() {
    return 0;
}